现在我想把这个项目改造成解析自定义的二进制文件的解析工具，我的目的是要把它改造成autosar中的nvm解析工具，它可以根据nvm的arxml配置来进行特殊的渲染，比如根据nvm的配置，找到对应的nvm block在二进制文件中的地址，并确认对应的长度，进行特殊的渲染，比如颜色不同，点击对应的nvm block数据可以显示对应的nvm配置





我现在想写一个算法，进行xml文件的比较，目前的比较工具beyond compare工具会让一些没有实际变化，如顺序变化，uuid变化，注释变化比较起来都比较困难，特别是xml文件比较打的时候，现在我想实现对xml文件深度比较，例如：同一个父元素下面的多个子元素的顺序变化可以将比较结果以未变化的形式呈现，其他的也类似，如果有现成的工具和开源工程麻烦也说明一下，如果没有给出对应的算法和实现